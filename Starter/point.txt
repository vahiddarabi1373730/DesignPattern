Proxy:
 یک الگوی ساختاری است که یک جایگزین یا نماینده برای یک شیء واقعی می‌سازد تا قبل، بعد یا به‌جای فراخوانی اصلِ شیء، یک منطق اضافه اجرا کند.
 
Proxy: 
واسطه‌ای است که مثل شیء اصلی رفتار می‌کند اما امکان اضافه کردن کنترل‌ها (cache, auth, log, lazy-load) قبل از فراخوانی واقعی را می‌دهد.
 